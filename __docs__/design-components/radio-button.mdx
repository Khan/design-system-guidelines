import {Meta} from "@storybook/addon-docs/blocks";
import {NeutralBadge} from "@khanacademy/wonder-blocks-badge";
import Figure from "../components/Figure";
import radioButtonImg from "./assets/radio-button/radio-button.png";
import defaultSelectionImg from "./assets/radio-button/default-selection.png";
import disabledImg from "./assets/radio-button/disabled.png";
import exposedImg from "./assets/radio-button/exposed.png";
import dropdownAltImg from "./assets/radio-button/dropdown-alt.png";
import filterBarDoImg from "./assets/radio-button/filter-bar-do.png";
import filterBarDontImg from "./assets/radio-button/filter-bar-dont.png";
import immediateSelectionImg from "./assets/radio-button/immediate-selection.png";
import noSelectionImg from "./assets/radio-button/no-selection.png";
import avoidNestingImg from "./assets/radio-button/avoid-nesting.png";

<Meta title="Design Guidelines / Components / Inputs / Radio button" />

<img src={radioButtonImg} alt="Radio button" />

# Radio button

<NeutralBadge label="Mutually exclusive selection" />

A mutually exclusive selection mechanism used to allow users to choose only one option from a small, visible set of choices.

## When to use

The choice to use radio buttons is determined by the need to present a small, visible set of mutually exclusive options where only one can be selected at a time.

| Use when | Example use |
| --- | --- |
| The selection is mutually exclusive — only one item can be chosen | Choosing Khanmigo’s voice: Voice 1, Voice 2, or Voice 3. |
| The options are few enough to compare side by side without opening a menu | Choosing an assignment status: To do, In progress, or Complete. |

## When NOT to use

| Don't use when | Recommended alternative | Rationale |
| --- | --- | --- |
| The user needs to select multiple items (A, B, and C) | Checkbox group | Radio buttons cannot be used when more than one item can be chosen at a time. |
| The list contains more than 8 options | Single-select dropdown | Beyond 8 options, scanning an exposed list costs more than opening a menu. |
| It's a simple, global on/off constraint that affects the entire view | Toggle | For a simple, global ON/OFF constraint, a toggle is visually clearer. |

## Exposed or dropdown

- 5 or fewer — always exposed.
- 6 to 8 — exposed by default. A dropdown is acceptable only when the options are familiar and ordered and the user doesn't need to compare them.
- More than 8 — dropdown.
- Top filter bar — always a dropdown, regardless of count.

<img src={exposedImg} alt="A modal with a short radio group exposed as a list — “To help with your children’s school learning” and “To homeschool your children”" />

<img src={dropdownAltImg} alt="A settings form where a long list of states is contained in a single-select dropdown showing “California”" />

### Top filter bar

Horizontal space is shared across several controls, so exposed options would break the layout.

<Figure
    src={filterBarDoImg}
    alt="A top filter bar where course options are contained in a single-select dropdown"
    caption="use a single-select dropdown for radio options in the top filter bar."
    type="do"
/>

<Figure
    src={filterBarDontImg}
    alt="A top filter bar with radio options exposed directly next to the filter dropdowns"
    caption="expose radio options directly in the top filter bar."
    type="dont"
/>

## States

### Default selection

Pre-select one option so the group is never in an ambiguous or unvalidated state.

The exception is a choice that must be deliberate — consent, a survey answer, or anything where "the user didn’t answer" is meaningful information. Pre-selecting makes it impossible to tell a real choice from an untouched form. In those cases, start with no option selected and treat the group as required.

<img src={defaultSelectionImg} alt="A radio group of four options with the first option pre-selected" />

### Disabled

Used when a prerequisite action has not been fulfilled. The field is grayed out and non-interactive. Once the dependency is met, it no longer displays as disabled. Do not use disabled when read-only is more appropriate — disabled implies unavailability, not restriction.

<img src={disabledImg} alt="A single radio option shown grayed out in its disabled state" />

## Interaction

### Immediate, single selection

Clicking any option immediately selects it and deselects the previously chosen option within the group.

<img src={immediateSelectionImg} alt="Two side-by-side radio groups showing selection moving from the first option to the second when clicked" />

### Returning to no selection

Once an option is selected, the user cannot return to a zero-selected state. Radio buttons have no mechanism for deselection — choosing one option only moves the selection elsewhere. If "none" is a legitimate answer, provide it as an explicit option rather than expecting the user to clear the group.

<Figure
    src={noSelectionImg}
    alt='A radio group — Voice 1, Voice 2, and Voice 3, each with a play button — with no option selected and no explicit "None" option'
    caption='omit a "None" option when "none" is a valid answer'
    type="dont"
/>

### Avoid nesting

Do not nest radio buttons within one another, as this creates complex dependencies and severely damages clarity.

<Figure
    src={avoidNestingImg}
    alt="A radio group with two options nested and indented beneath a parent option"
    caption="nest radio buttons within one another."
    type="dont"
/>
