import {Meta} from "@storybook/addon-docs/blocks";
import progressStepperImg from "./assets/progress-stepper/progress-stepper.png";
import variants1Img from "./assets/progress-stepper/variants-1.png";
import variants2Img from "./assets/progress-stepper/variants-2.png";
import copyImg from "./assets/progress-stepper/copy.png";
import {NeutralBadge} from "@khanacademy/wonder-blocks-badge";

<Meta title="Design Guidelines / Components / Feedback / Progress stepper" />

<img src={progressStepperImg} alt="Progress stepper" />

# Progress stepper

<NeutralBadge label="Multi-step flow" /> Communicates a user's position within a multi-step, sequential flow, signaling how much of the process remains. Two variants exist based on flow complexity and page context.

## When to use

The choice to use a progress stepper is determined by the need to guide a user through a defined sequence of steps where order matters and progress visibility reduces abandonment or errors.

| Use when | Example use |
| --- | --- |
| The flow has 3–6 discrete, ordered steps | Onboarding (4 screens), assign content flow (4 named stages) |
| Steps are sequential — each one leads to the next, whether it requires input or is informational | The assign content flow, where each stage depends on decisions made in the previous one |
| The flow has a clear, known endpoint | A multi-step flow ending in a Review + Confirm step |

## When NOT to use

| Don’t use when | Recommended alternative | Rationale |
| --- | --- | --- |
| Steps can be completed in any order | Checklist or task list | A stepper implies sequence. Non-linear tasks create confusion about whether order is enforced. |
| 2 steps or fewer | Single-page form or modal | A stepper adds structural overhead that isn't justified for minimal flows. |
| The system is doing the work, not the user | Loading state | A stepper implies user-driven advancement. System-driven progress needs a different pattern. |

## Variants

### Simple

Segmented bars + numeric counter. No step labels. These are visual indicators only, they are not clickable to navigate between.
Use for lightweight, motivational flows like onboarding where steps don't need to be named.

Anchored - bottom left

<img src={variants1Img} alt="Variants 1" />

### Complex

Named steps with completion indicators and a progress underline. 
Use when steps have distinct names, the flow is task-critical, or a user may need to navigate back to a previous step.

Anchored - center top nav

<img src={variants2Img} alt="Variants 2" />

## Copy

Applies to step labels in the complex variant only.
Keep labels to 1–2 words. Use nouns, not verbs — the label orients the user, it doesn't instruct them. Write in sentence case.

<img src={copyImg} alt="Copy" />

---

[Figma source](https://www.figma.com/design/G9Yxa0SOlALS078wtq87cM/Design-system-guidelines---Thunderblocks?node-id=13456-29014)
